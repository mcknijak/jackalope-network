# Copy to .env and fill in. Both values should come from a password manager.

# Postgres password for the paperless database. Generate with:
#   openssl rand -hex 24
PAPERLESS_DB_PASS=

# Django secret key. Generate with:
#   openssl rand -hex 50
PAPERLESS_SECRET_KEY=
